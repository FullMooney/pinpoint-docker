apiVersion: v1
kind: Service
metadata:
  name: {{ include "zookeeper.fullname" . }}1
  labels:
    {{- include "zookeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: zookeeper
  selector:
    {{- include "zookeeper.selectorLabels" . | nindent 4 }}